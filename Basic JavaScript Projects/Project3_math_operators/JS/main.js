// A function that performs an addition operation
function additionFunction() {
    // Perform addition and store the result
    var result = 5 + 7;
    // Display the result in the paragraph element with id "Math"
    document.getElementById("Math").innerHTML = "5 + 7 = " + result;
}
